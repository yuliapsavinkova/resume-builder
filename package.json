{
  "name": "resume",
  "version": "1.0.0",
  "description": "Resume builder, generates a .docx from YAML.",
  "type": "module",
  "scripts": {
    "build": "tsx src/index.ts"
  },
  "notes": {
    "usage": "pnpm build [variant]"
  },
  "dependencies": {
    "docx": "^9.5.3",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
